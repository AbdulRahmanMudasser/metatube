import 'package:flutter/material.dart';

class AppColors {
  /// Dark color scheme for the application
  static const Color dark = Color(0xFF1E1E1E);

  /// Light color scheme for the application
  static const Color light = Color(0xFFFFFFFF);

  /// Medium color scheme for the application
  static const Color medium = Color(0x50FFFFFF);

  /// Accent color scheme for the application
  static const Color accent = Color(0xFFFF9900);

  // Background color for disabled elements
  static const Color disabledBackgroundColor = Colors.black12;

  // Foreground color for disabled elements
  static const Color disabledForegroundColor = Colors.white12;
}